spring.profiles.active=@activeProfile@
server.session.tracking-modes=cookie

spring.mvc.pathmatch.matching-strategy=ant_path_matcher

#jwt
jwt.token.secret=${MHELPER_JWT_SECRET:Jwt2257Syndrome}
jwt.token.expired=2592000000
jwt.header=Authorization

#Telegram

bot.time=5
bot.max_attempts=100

#Spam Protect
spam.protected.range=12 

#Daily Scheduled
scheduler.delay=3000
scheduler.interval_between_launches=PT24H
